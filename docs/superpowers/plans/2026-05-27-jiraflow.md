# JiraFlow MCP Implementation Plan

> **For agentic workers:** Execute checkbox tasks in order. Run `npm run build` after each phase.

**Goal:** JiraFlow v2 — eight PRD tools, workflow state, hosted workspaces, git/MR.

**Architecture:** `src/jiraflow/*` modules + `src/jira/issue-context.ts`; thin `server-core.ts`.

**Tech Stack:** TypeScript, MCP SDK, yaml, simple-git, minimatch, node:test.

---

## Phase 0 — Docs

- [x] Design spec: `docs/superpowers/specs/2026-05-27-jiraflow-design.md`
- [x] This plan file

## Phase 1 — Foundation

- [x] `src/jiraflow/response.ts`, `config.ts`, `state.ts`, `workspace.ts`
- [x] Register tool stubs in `tools.ts`
- [x] Unit tests

## Phase 2 — Context

- [x] `src/jira/issue-context.ts` extract bundle fetch
- [x] `intelligence.ts`, `context-engine.ts`, `plan.ts`, `adf.ts`
- [x] `jira_start_ticket` full implementation

## Phase 3 — Git

- [x] `git.ts` safe_git
- [x] workspace_setup, create_feature_branch, commit_with_context
- [x] Device store git tokens + setup UI fields

## Phase 4 — Ship

- [x] `approval.ts`, `validate.ts`, `mr.ts`, `audit.ts`
- [x] create_merge_request, state transitions

## Phase 5 — Polish

- [x] Legacy wrapper, README, examples, version 2.0.0
- [x] `npm run build` + `npm test`
